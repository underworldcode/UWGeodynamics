name: tests

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: docker://underworldcode/underworld2
      - name: Impoet Underworld
        run: python -c "import underworld"